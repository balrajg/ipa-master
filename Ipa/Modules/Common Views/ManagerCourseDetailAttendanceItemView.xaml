<?xml version="1.0" encoding="UTF-8"?>
<ContentView 
xmlns="http://xamarin.com/schemas/2014/forms" 
xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml" 
xmlns:Views="clr-namespace:Ipa;assembly=Ipa" 
x:Class="Ipa.ManagerCourseDetailAttendanceItemView">
<ContentView.Resources>
		<ResourceDictionary>
			<OnPlatform x:TypeArguments="x:Int32" Android="66" iOS="60" x:Key="HeaderRowHeight" />
			</ResourceDictionary>
			</ContentView.Resources>
	<ContentView.Content>
		<StackLayout HorizontalOptions="FillAndExpand" Spacing="0">
			<ListView 
				x:Name="AttendanceList" 
				ItemsSource="{Binding AttendanceListItems}" 
				SelectedItem="{Binding SelectedItem, Mode=TwoWay}" 
				SeparatorVisibility="None" 
				RowHeight="{StaticResource HeaderRowHeight}"
				BackgroundColor="White"
				HorizontalOptions="FillAndExpand" 
				VerticalOptions="FillAndExpand">
				<ListView.ItemTemplate>
					<DataTemplate>
						<ViewCell>
							<ViewCell.View>
								<Views:AttendanceItemView ShowDay="{Binding ClassDay}" ShowDate="{Binding ClassDate}" PresentStatus="{Binding TotalPresent}" AbsentStatus="{Binding TotalAbsent}" />
							</ViewCell.View>
						</ViewCell>
					</DataTemplate>
				</ListView.ItemTemplate>
			</ListView>
		</StackLayout>
	</ContentView.Content>
</ContentView>